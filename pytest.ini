[pytest]
DJANGO_SETTINGS_MODULE = cbc_backend.settings
python_files = tests/test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto

# Markers
markers =
    unit: Fast, no DB, no network — pure logic
    integration: Uses the test DB (slower)
